#include <iostream>
#include <vector>
#define rep(i, n) for (int i = 0; i < n; i++)
using namespace std;
int main(void) {
    int N, M;
    cin >> N >> M;
    vector<int> w(N);
    rep(i, N) cin >> w[i];
    vector<vector<bool>> dp();
}