// Multiply and Rotate (緑色, 400 点)
// BFSを使って解く
// 最小回数を求めるには BFS！！(

#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int main(void) {
    ll a, N;
    cin >> a >> N;
    queue<ll> queue;
    vector<ll> dp(N * 10, -1);
}